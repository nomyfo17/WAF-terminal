# WAF 🥞
To install this tool globally:
1. Clone the repo: `git clone https://github.com/nomyfo17/waf-terminal`
2. Run the installer: `chmod +x install.sh && ./install.sh`
3. Usage: Just type `WAF` in your terminal!
